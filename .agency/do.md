# /do config

## Check command
`nix build` — builds the default package (which runs shellcheck on the script via `writeShellApplication`).

## Documentation
Keep `README.md` in sync with user-facing changes.

<!-- Optional (add manually for the evidence step):
## PR evidence
-->
